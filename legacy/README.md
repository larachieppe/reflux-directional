# Superseded

`build_full_report.py` generated FULL_REPORT.html from prose embedded in a Python
f-string. It is replaced by `report/*.md` (prose) plus `report_data.py` (numbers)
rendered by `build_report_md.py`.

Kept only for reference. Do not run it: it would overwrite FULL_REPORT.html from
a stale copy of the prose.
